<Window x:Class="TaskbarFolderShortcut.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Folder Tray App" Height="0" Width="0"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <ContextMenu x:Key="RootContextMenu" x:Name="RootContextMenu" 
                     ItemTemplate="{StaticResource FileSystemItemTemplate}"
                     ItemContainerStyle="{StaticResource FileSystemMenuItemStyle}"
                     Placement="MousePoint"
                     Closed="RootContextMenu_Closed">
        </ContextMenu>
    </Window.Resources>
    <Grid>
    </Grid>
</Window>
